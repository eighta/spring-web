package a8.test.research;

import java.io.ByteArrayOutputStream;
import java.io.DataOutputStream;
import java.io.IOException;

import org.junit.Test;

//https://es.wikipedia.org/wiki/Hadoop
public class HadoopTest {

	@Test
	public void probarFramework() throws IOException{
		
			//SERIALIZATION
		/*
	      //Instantiating the IntWritable object
	      IntWritable intwritable = new IntWritable(12);
	   
	      //Instantiating ByteArrayOutputStream object
	      ByteArrayOutputStream byteoutputStream = new ByteArrayOutputStream();
	   
	      //Instantiating DataOutputStream object
	      DataOutputStream _dataOutputStream = new DataOutputStream(byteoutputStream);
	   
	      //Serializing the data
	      intwritable.write(_dataOutputStream);
	   
	      //storing the serialized object in bytearray
	      byte[] byteArray = byteoutputStream.toByteArray();
	   
	      //Closing the OutputStream
	      _dataOutputStream.close();
	      
	      */
	      
	      
	   }
}
